{
  "name": "campus-connect",
  "private": false,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "admin:dev": "turbo dev --workspace=admin",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.1.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "commitlint": "19.3.0",
    "husky": "^9.0.11",
    "prettier-plugin-tailwindcss": "^0.6.0"
  }
}